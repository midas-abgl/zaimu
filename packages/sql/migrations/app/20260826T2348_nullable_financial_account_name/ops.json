[
	{
		"execute": [
			{
				"description": "drop NOT NULL on \"name\"",
				"sql": "ALTER TABLE \"public\".\"FinancialAccount\" ALTER COLUMN \"name\" DROP NOT NULL"
			}
		],
		"id": "alterNullability.dropNotNull.FinancialAccount.name",
		"label": "Drop NOT NULL on \"FinancialAccount\".\"name\"",
		"operationClass": "widening",
		"postcheck": [
			{
				"description": "verify column \"name\" is nullable",
				"params": ["public", "FinancialAccount", "name", "YES"],
				"sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3 AND \"is_nullable\" = $4)) AS \"result\""
			}
		],
		"precheck": [
			{
				"description": "ensure column \"name\" exists",
				"params": ["public", "FinancialAccount", "name"],
				"sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\""
			}
		],
		"target": {
			"details": {
				"name": "name",
				"objectType": "column",
				"schema": "public",
				"table": "FinancialAccount"
			},
			"id": "postgres"
		}
	}
]
