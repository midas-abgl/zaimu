[
	{
		"execute": [
			{
				"description": "add column \"isHidden\"",
				"sql": "ALTER TABLE \"public\".\"Transaction\" ADD COLUMN \"isHidden\" bool DEFAULT false NOT NULL"
			}
		],
		"id": "column.public.Transaction.isHidden",
		"label": "Add column \"isHidden\" to \"Transaction\"",
		"operationClass": "additive",
		"postcheck": [
			{
				"description": "verify column \"isHidden\" exists",
				"params": ["public", "Transaction", "isHidden"],
				"sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\""
			}
		],
		"precheck": [
			{
				"description": "ensure column \"isHidden\" is missing",
				"params": ["public", "Transaction", "isHidden"],
				"sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\""
			}
		],
		"target": {
			"details": {
				"name": "isHidden",
				"objectType": "column",
				"schema": "public",
				"table": "Transaction"
			},
			"id": "postgres"
		}
	}
]
