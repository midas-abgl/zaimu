[
	{
		"execute": [
			{
				"description": "add column \"storeName\"",
				"sql": "ALTER TABLE \"public\".\"RecurringPayment\" ADD COLUMN \"storeName\" character varying(200)"
			}
		],
		"id": "column.public.RecurringPayment.storeName",
		"label": "Add column \"storeName\" to \"RecurringPayment\"",
		"operationClass": "additive",
		"postcheck": [
			{
				"description": "verify column \"storeName\" exists",
				"params": ["public", "RecurringPayment", "storeName"],
				"sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\""
			}
		],
		"precheck": [
			{
				"description": "ensure column \"storeName\" is missing",
				"params": ["public", "RecurringPayment", "storeName"],
				"sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\""
			}
		],
		"target": {
			"details": {
				"name": "storeName",
				"objectType": "column",
				"schema": "public",
				"table": "RecurringPayment"
			},
			"id": "postgres"
		}
	},
	{
		"execute": [
			{
				"description": "add column \"storeName\"",
				"sql": "ALTER TABLE \"public\".\"Subscription\" ADD COLUMN \"storeName\" character varying(200)"
			}
		],
		"id": "column.public.Subscription.storeName",
		"label": "Add column \"storeName\" to \"Subscription\"",
		"operationClass": "additive",
		"postcheck": [
			{
				"description": "verify column \"storeName\" exists",
				"params": ["public", "Subscription", "storeName"],
				"sql": "SELECT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\""
			}
		],
		"precheck": [
			{
				"description": "ensure column \"storeName\" is missing",
				"params": ["public", "Subscription", "storeName"],
				"sql": "SELECT NOT EXISTS (SELECT 1 AS \"one\" FROM \"information_schema\".\"columns\" WHERE (\"table_schema\" = $1 AND \"table_name\" = $2 AND \"column_name\" = $3)) AS \"result\""
			}
		],
		"target": {
			"details": {
				"name": "storeName",
				"objectType": "column",
				"schema": "public",
				"table": "Subscription"
			},
			"id": "postgres"
		}
	}
]
